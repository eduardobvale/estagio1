/**
 * Configuration variables.
 */

 CE = {};

//game state
CE.GAME_STATE = {
    HOME:0,
    PLAY:1,
    OVER:2
};

// Pixel to Meters Ratio
CE.PTM_RATIO = 10;

// Z Order
CE.Z_ORDER = [0,1,2,3];

//keys
CE.KEYS = [];

CE.KEYS_ALIAS = { UP: 38, DASH: 32 };

//level
CE.LEVEL = {
    STAGE1:1,
    STAGE2:2,
    STAGE3:3
};

//life
CE.LIFE = 4;

//score
CE.SCORE = 0;

//sound
CE.SOUND = true;
